# AGENTS.md — Algoricast

Read `CLAUDE.md` first — it is law here, whichever agent you are. All
golden rules bind you, plus rule 11 (features enter only through
`features/` specs). Note rule 8 especially: client-facing output is
Claude-only. Non-Claude agents transplant user-visible strings verbatim
from approved copy decks (`features/*/COPY` sections or COPY.md files) and
never author them. Plumbing, tests, migrations, identifiers: any agent.
Voice: one family. Agent commits carry co-author trailers — provenance
applies to the builders of the provenance machine too.
